{
    "configurations": [
        {
            "name": "standard debug",
            "type": "stlinkgdbtarget",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "preBuild": "${command:st-stm32-ide-debug-launch.build}",
            "runEntry": "main",
            "imagesAndSymbols": [
                {
                    "imageFileName": "${workspaceFolder}/build/Debug/nixie_clock_V2.elf"
                }
            ],
            "verbose": false
        }
    ]
}